/**
 * Shared hooks - 엔티티와 무관한 범용 훅들
 */

// Storage hooks
export { useLocalStorage } from "./useLocalStorage";

// Search hooks (범용적으로 사용 가능)
export { useSearch } from "./useSearch";

// Format hooks (UI 관련 범용 훅)
export { useFormatPrice } from "./useFormatPrice";
