/**
 * Constants - 새로운 도메인 구조로 re-export
 */

// Product domain constants (migrated to domain/product/constants)
export { initialProducts } from "../domain/product/constants";

// Coupon domain constants (migrated to domain/coupon/constants)
export { initialCoupons } from "../domain/coupon/constants";
